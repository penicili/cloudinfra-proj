[project]
name = "app"
version = "0.1.0"
description = "URL Shortener — UAS Infrastruktur Awan"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.3",
    "redis>=5.0",
    "waitress>=3.0.2",
]
